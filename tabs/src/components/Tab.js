import React from "react";
import { Button, TabContainer } from "./Tab.styles";

const Tab = () => {
  return (
    <TabContainer>
      <Button>Tommy</Button>
      <Button>Big</Button>
      <Button>cuck</Button>
    </TabContainer>
  );
};

export default Tab;
